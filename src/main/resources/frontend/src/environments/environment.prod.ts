// Configuración de entorno para producción
export const environment = {
  production: true,
  apiUrl: '/api', // En producción, las llamadas van al mismo origen
  backendUrl: '/api', // URL del backend para el interceptor
  appName: 'Caribe Vibes',
  version: '1.0.0'
};
